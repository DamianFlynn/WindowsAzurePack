=== PKI View
Running PKIVIEW we can perform a check to ensure that our PKI environment is configured correctly and that the certificates are all valid, including the CRLs.

To launch the tool execute the following command
```powershell
pkiview.msc
```
